import { SanitizeSrcPipe } from './sanitize-src.pipe';

describe('SanitizeSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizeSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
