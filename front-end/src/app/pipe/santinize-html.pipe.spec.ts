import { SantinizeHtmlPipe } from './santinize-html.pipe';

describe('SantinizeHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new SantinizeHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
